package com.developmentontheedge.beans;

public interface IndexedPropertyConstants
{
    /**
     * RESIZABLE attrbute in dynamic and indexed properties specifies
     * that new elements can be added at run time
     */
    String RESIZABLE = "array: resizable";

    /**
     * CAN_DELETE attrbute in dynamic and indexed properties specifies
     * that elements can be deleted at run time
     */
    public static final String CAN_DELETE = "array: can delete";

    /**
     * CAN_INSERT attrbute in dynamic and indexed properties specifies
     * that new elements can be inserted at run time
     */
    public static final String CAN_INSERT = "array: can insert";

    /**
     * CAN_MOVE attrbute in dynamic and indexed properties specifies
     * that an order of elements can be changed at run-time
     */
    public static final String CAN_MOVE = "array: can move";
}


